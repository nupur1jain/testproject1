pipeline {
  agent any
  stages {
    stage('build') {
      steps {
        echo 'build is created'
      }
    }
    stage('test') {
      steps {
        echo 'testing has to be done'
      }
    }
    stage('deploy') {
      steps {
        echo 'deploy done '
      }
    }
  }
}